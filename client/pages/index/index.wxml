<!--index.wxml-->
<view class="container">
    <map longitude="114.31665" latitude="30.554408"></map>
    <navigator url="../collection/collection" open-type="navigate">
      <button id="addCollection"></button>
    </navigator>
    <view class="template">
      <button wx:for="{{templates}}" wx:key="{{item.name}}">{{item.name}}</button>
    </view>
</view>
